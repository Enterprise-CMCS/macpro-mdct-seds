import React from "react";

const QuartersList = () => {
  // // let quarters = "<ul>";
  // // for (const quarter in years[year].quarters) {
  // //   quarters += "<li>Quarter" + years[year].quarters[quarter] + "</li>";
  // // }
  // quarters += "</ul>";

  return (
    <ul>
      <li>here is an item</li>
      <li>her item 2</li>
    </ul>
  );
};

export default QuartersList;
